{% extends 'base.html.twig' %}

{% block body %}
    <h1>SettingsShperndarjaPunimitTokes</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ settingsShperndarjaPunimitToke.id }}</td>
            </tr>
            <tr>
                <th>Shperndarja</th>
                <td>{{ settingsShperndarjaPunimitToke.shperndarja }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('settingsshperndarjapunimittokes_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('settingsshperndarjapunimittokes_edit', { 'id': settingsShperndarjaPunimitToke.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
