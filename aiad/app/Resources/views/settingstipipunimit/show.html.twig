{% extends 'base.html.twig' %}

{% block body %}
    <h1>SettingsTipiPunimit</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ settingsTipiPunimit.id }}</td>
            </tr>
            <tr>
                <th>Tipipunimit</th>
                <td>{{ settingsTipiPunimit.tipiPunimit }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('settingstipipunimit_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('settingstipipunimit_edit', { 'id': settingsTipiPunimit.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
