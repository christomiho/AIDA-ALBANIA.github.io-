{% extends 'base.html.twig' %}

{% block body %}
    <h1>SettingsMenyraEleminimit</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ settingsMenyraEleminimit.id }}</td>
            </tr>
            <tr>
                <th>Menyra</th>
                <td>{{ settingsMenyraEleminimit.menyra }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('settingsmenyraeleminimit_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('settingsmenyraeleminimit_edit', { 'id': settingsMenyraEleminimit.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
