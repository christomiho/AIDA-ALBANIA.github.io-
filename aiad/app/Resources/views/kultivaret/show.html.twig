{% extends 'base.html.twig' %}

{% block body %}
    <h1>Kultivaret</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kultivaret.id }}</td>
            </tr>
            <tr>
                <th>Kultivari</th>
                <td>{{ kultivaret.kultivari }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('kultivaret_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('kultivaret_edit', { 'id': kultivaret.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
