{% extends 'AiadBundle:Default:base.html.twig' %}

{% block content %}
    {% include 'AiadBundle:Default:header.html.twig' %}
    {% include 'AiadBundle:Default:left_sidebar.html.twig' %}
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>Aplikime Sëmundje</h1>
    </section>

    <!-- Main content -->
    <section class="content">

        <div class="row">
            <div class="col-md-12">

                <!-- TABLE: LATEST ORDERS -->
                <div class="box box-info">
                    <div class="box-header with-border">
                        <h3 class="box-title">Tabela Përmbledhese</h3>
                        <div class="box-tools pull-right">

                        </div>
                    </div><!-- /.box-header -->
                    <div class="box-body">
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}

        <input class="btn btn-success pull-right" type="submit" value="Shto" />
    {{ form_end(edit_form) }}


            {#<a class="btn btn-info pull-left" href="{{ path('tabelapermbledheseaplikimsemundjesh_index') }}">Back to the list</a>#}

            {#{{ form_start(delete_form) }}#}
                {#<input type="submit" value="Delete">#}
            {#{{ form_end(delete_form) }}#}

                    </div><!-- /.box-body -->
                </div><!-- /.box -->
            </div>
        </div>
    </section><!-- Main content -->
</div><!-- content wrapper-->
    {% include 'AiadBundle:Default:right_sidebar.html.twig' %}
{% endblock %}
{% block javascripts %}

    <script type="text/javascript">
        var x = "{{ app.session.get('parcelId') }}";
        $('#tabela_permbledhese_aplikim_semundjesh_parcela').closest('.form-group').css('display', 'none');
        $('#tabela_permbledhese_aplikim_semundjesh_parcela option[value = '+ x +']').attr('selected','selected');

    </script>


{% endblock %}



