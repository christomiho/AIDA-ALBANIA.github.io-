{% extends 'base.html.twig' %}

{% block body %}
    <h1>SettingsGrackaFerromoni</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ settingsGrackaFerromoni.id }}</td>
            </tr>
            <tr>
                <th>Ferromoni</th>
                <td>{{ settingsGrackaFerromoni.ferromoni }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('settingsgrackaferromoni_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('settingsgrackaferromoni_edit', { 'id': settingsGrackaFerromoni.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
