{% extends 'base.html.twig' %}

{% block body %}
    <h1>SettingsPleherimiTipi</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ settingsPleherimiTipi.id }}</td>
            </tr>
            <tr>
                <th>Tipipleherimit</th>
                <td>{{ settingsPleherimiTipi.tipiPleherimit }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('settingspleherimitipi_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('settingspleherimitipi_edit', { 'id': settingsPleherimiTipi.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
