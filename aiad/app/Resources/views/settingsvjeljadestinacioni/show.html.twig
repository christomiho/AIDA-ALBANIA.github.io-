{% extends 'base.html.twig' %}

{% block body %}
    <h1>SettingsVjeljaDestinacioni</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ settingsVjeljaDestinacioni.id }}</td>
            </tr>
            <tr>
                <th>Destinacioni</th>
                <td>{{ settingsVjeljaDestinacioni.destinacioni }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('settingsvjeljadestinacioni_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('settingsvjeljadestinacioni_edit', { 'id': settingsVjeljaDestinacioni.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
