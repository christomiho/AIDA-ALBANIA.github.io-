{% extends 'base.html.twig' %}

{% block body %}
    <h1>SettingsModeliPleherimit</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ settingsModeliPleherimit.id }}</td>
            </tr>
            <tr>
                <th>Modelipleherimit</th>
                <td>{{ settingsModeliPleherimit.modeliPleherimit }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('settingsmodelipleherimit_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('settingsmodelipleherimit_edit', { 'id': settingsModeliPleherimit.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
