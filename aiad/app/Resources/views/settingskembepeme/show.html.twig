{% extends 'base.html.twig' %}

{% block body %}
    <h1>SettingsKembePeme</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ settingsKembePeme.id }}</td>
            </tr>
            <tr>
                <th>Nrkembepeme</th>
                <td>{{ settingsKembePeme.nrKembePeme }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('settingskembepeme_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('settingskembepeme_edit', { 'id': settingsKembePeme.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
