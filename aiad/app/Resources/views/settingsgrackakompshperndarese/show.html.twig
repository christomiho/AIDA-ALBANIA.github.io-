{% extends 'base.html.twig' %}

{% block body %}
    <h1>SettingsGrackaKompShperndarese</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ settingsGrackaKompShperndarese.id }}</td>
            </tr>
            <tr>
                <th>Emrikompshperndarese</th>
                <td>{{ settingsGrackaKompShperndarese.emriKompShperndarese }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('settingsgrackakompshperndarese_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('settingsgrackakompshperndarese_edit', { 'id': settingsGrackaKompShperndarese.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
