{% extends 'base.html.twig' %}

{% block body %}
    <h1>SettingsFirmaProdukteve</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ settingsFirmaProdukteve.id }}</td>
            </tr>
            <tr>
                <th>Emrifirmes</th>
                <td>{{ settingsFirmaProdukteve.emriFirmes }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('settingsfirmaprodukteve_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('settingsfirmaprodukteve_edit', { 'id': settingsFirmaProdukteve.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
