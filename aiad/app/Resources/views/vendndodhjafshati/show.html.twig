{% extends 'base.html.twig' %}

{% block body %}
    <h1>VendndodhjaFshati</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vendndodhjaFshati.id }}</td>
            </tr>
            <tr>
                <th>Emrifshati</th>
                <td>{{ vendndodhjaFshati.emriFshati }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vendndodhjafshati_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vendndodhjafshati_edit', { 'id': vendndodhjaFshati.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
