{% extends 'base.html.twig' %}

{% block body %}
    <h1>SettingsGrackaKompProdhuse</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ settingsGrackaKompProdhuse.id }}</td>
            </tr>
            <tr>
                <th>Emrikompanise</th>
                <td>{{ settingsGrackaKompProdhuse.emriKompanise }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('settingsgrackakompprodhuse_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('settingsgrackakompprodhuse_edit', { 'id': settingsGrackaKompProdhuse.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
