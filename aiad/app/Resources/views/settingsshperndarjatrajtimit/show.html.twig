{% extends 'base.html.twig' %}

{% block body %}
    <h1>SettingsShperndarjaTrajtimit</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ settingsShperndarjaTrajtimit.id }}</td>
            </tr>
            <tr>
                <th>Shperndarjatrajtimit</th>
                <td>{{ settingsShperndarjaTrajtimit.shperndarjaTrajtimit }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('settingsshperndarjatrajtimit_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('settingsshperndarjatrajtimit_edit', { 'id': settingsShperndarjaTrajtimit.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
