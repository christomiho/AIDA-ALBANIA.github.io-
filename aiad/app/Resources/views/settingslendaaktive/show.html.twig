{% extends 'base.html.twig' %}

{% block body %}
    <h1>SettingsLendaAktive</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ settingsLendaAktive.id }}</td>
            </tr>
            <tr>
                <th>Emrilendesaktive</th>
                <td>{{ settingsLendaAktive.emriLendesAktive }}</td>
            </tr>
            <tr>
                <th>Pastertialendes</th>
                <td>{{ settingsLendaAktive.pastertiaLendes }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('settingslendaaktive_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('settingslendaaktive_edit', { 'id': settingsLendaAktive.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
